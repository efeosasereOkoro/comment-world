# Public Supabase config baked into the widget build.
# These are PUBLIC values (the publishable/anon key is safe to expose in the browser);
# access is controlled by Row Level Security + the origin-checked write path, not secrecy.
# Real values are filled in once the fresh Supabase project exists (Phase 3).
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
